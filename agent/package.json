{
  "name": "biltest-quote-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Home worker agent for the Build in Lombok quote engine (outbound-only; no deps).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
